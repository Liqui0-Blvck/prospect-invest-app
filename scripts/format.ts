// export function format(fecha: number | Date | undefined, opciones = {}) {
//   return new Intl.DateTimeFormat('es-LA', {
//     day: 'numeric',
//     month: 'long',
//     year: 'numeric',
//     ...opciones  // Permite añadir más opciones como hora, minuto, etc.
//   }).format(fecha);
// }
